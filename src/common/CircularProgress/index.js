import { CircularProgress } from '@mui/material';

const CircularProgressLoading = ({ size, color }) => {
  return <CircularProgress size={size} color={color} />;
};
export default CircularProgressLoading;
